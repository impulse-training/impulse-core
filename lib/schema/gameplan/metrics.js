"use strict";
// import * as yup from 'yup';
// import { gameplanBaseSchema } from './base';
// // In future, we may have gameplans that are triggered in response to metrics, e.g. when feeling sad
// export const metricsGameplanSchema = gameplanBaseSchema('metrics').shape({
//   metrics: yup.array().of(yup.string()).required().min(1),
// });
// export type EmotionsGameplanValue = yup.InferType<typeof metricsGameplanSchema>;
